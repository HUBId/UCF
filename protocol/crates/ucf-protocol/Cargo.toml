[package]
name = "ucf-protocol"
version = "0.1.0"
edition = "2021"

[lib]
name = "ucf"
path = "src/lib.rs"

[dependencies]
blake3 = "1"
prost = "0.12"
serde = { workspace = true, features = ["derive"] }

[dev-dependencies]
serde_json = "1"
