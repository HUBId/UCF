[package]
name = "dbm_hpa"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
dbm_core = { path = "../dbm_core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
