# Serie BB31 Prompt 1: first anatomical stabilization line (maintenance hardening)

Status: Die erste anatomische Region (`hippocampus_like_region`) bleibt im maintenance-only Scope und wird gezielt gegen Semantik- und Surface-Drift gehärtet, ohne zweite anatomische Region und ohne Capability-Ausweitung.

## Canonical first-anatomical stabilization map

1. stable first-anatomical baseline
2. maintenance-hardened anatomical surface
3. maintenance-hardened diagnostics path
4. maintenance-hardened contract path
5. maintenance-hardened model boundary
6. non-canonical/internal-only residual path

Diese Map bleibt schmal; es wird keine neue Governance-/Planner-/Neuroplattform geöffnet.

## Surface semantics (drift-resistant)

- input/state/output/reference surfaces behalten ihre kanonische Bedeutung.
- advisory-only remains advisory-only.
- reference-only remains reference-only.
- Die anatomische Surface bleibt nicht-autoritativ und darf nicht in direkte Steuerung umgedeutet werden.

## Diagnostics, contract, and model semantics

- advisory-only, caveated, deferred, blocked, insufficient und diagnostic-only bleiben explizit getrennt.
- runtime/selection/reference lesen denselben diagnostischen Kern ohne parallele Semantik.
- current model mode bleibt `abstract functional current mode`.
- `bounded Kuramoto-like`, `HH simulation-only/diagnostic-only`, `HH-later` bleiben klar abgegrenzt und nicht als produktive Autorität auslegbar.

## Guard rails (maintenance-frozen)

Unverändert hart:
- no direct action trigger
- no direct execution trigger
- no direct retry trigger
- no direct memory commit
- no direct compute invocation
- no safety override

Zusätzlich unverändert außerhalb dieses Schritts:
- no second anatomical region
- keine implizite globale Modellplattform
- keine HH-Produktivintegration

## Residual/non-canonical handling

- non-canonical/internal-only paths bleiben explizit residual und nicht-operativ.
- test/helper Restpfade dürfen keine zweite operative Wirklichkeit erzeugen.
- Dokumentation, Contract-Mapping und Guards werden auf dieselbe stabile Deutung festgezogen.

## Maintenance baseline mirror

Die erste anatomische Region ist in die bestehende BB23/BB29 Freeze-/Maintenance-Baseline eingeordnet:
- maintenance-only Änderungen sind erlaubt,
- frozen no-direct-* Semantik bleibt bindend,
- semantische Re-Scopes benötigen explizite neue Serie-Entscheidung.
