# Release Notes

## ucf-coherence-v1

- Added deterministic `ucf-demo` CLI to run N coherence cycles with `--seed` and print one summary line per cycle.
- Added additive `CoherenceSummary` accessor in workspace (`coherence_summary`) for runtime/test reuse.
- Added operator runbook entry at `docs/ops/DEMO.md` and README demo quickstart.
