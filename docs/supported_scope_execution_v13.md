# Supported Scope Execution v13

`SupportedScopeExecutionV8` bleibt als v13-Artefakt dokumentiert, ist aber durch v14 abgelöst.

## Hinweis auf v14

Die aktuelle Ausführungshierarchie ist jetzt in `docs/supported_scope_execution_v14.md` definiert.

## v13-Status

- `SupportedScopeExecutionV8` war die autoritative Ausführungsentscheidung in v13.
- Ab v14 wird Erweiterung/Freeze nur noch über `SupportedScopeExecutionV9` autorisiert.
- v13-Artefakte bleiben für Audit/Lineage erhalten, dürfen aber nicht mehr direkt Applied-Scope-Änderungen treiben.
