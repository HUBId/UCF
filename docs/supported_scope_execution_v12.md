# Supported Scope Execution v12

This document is retained for historical continuity.

In v13, `SupportedScopeExecutionV8` supersedes `SupportedScopeExecutionV7` as the only execution artifact that can authorize `models supported-set-apply`.

Use `docs/supported_scope_execution_v13.md` for the current path.
